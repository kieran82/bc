{
  "name": "datacreation",
  "version": "1.0.0",
  "description": "A utility project to create JSON files for load testing as well as end use on browsers, etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-stringbuilder": "^2.2.4"
  },
  "devDependencies": {
    "log4js": "^6.4.0"
  }
}
